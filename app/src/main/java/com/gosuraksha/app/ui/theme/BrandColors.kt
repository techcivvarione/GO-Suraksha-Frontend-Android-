package com.gosuraksha.app.ui.theme

import androidx.compose.ui.graphics.Color

object BrandColors {

    // 🌱 Brand light green (primary accent everywhere)
    val GreenPrimary = Color(0xFF6EE48E)

    // 🌞 Light mode background
    val LightGreenBg = Color(0xFFE9F9EF)
    val White = Color.White

    // 🌙 Dark mode background
    val Black = Color.Black

    // 🧊 Glass card colors
    val GlassLight = Color.White.copy(alpha = 0.85f)
    val GlassDark = Color.Black.copy(alpha = 0.55f)
}
